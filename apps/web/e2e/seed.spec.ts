// Seed for Playwright Test Agents (planner / generator / healer).
//
// Generated specs reference this file as their baseline state ("Seed:
// e2e/seed.spec.ts" in plan markdown). Treat it as the fixture inheritance
// point: per-spec setup goes in the generated spec, common state goes here.
//
// Intentionally empty at install time. Live GitHub fixtures (the [E2E] Test
// Fixture milestone+issue) stay in happy-path.spec.ts — see ADR 0011 for the
// reasoning.
//
// When a generator-authored spec needs a baseline browser state, extend
// Playwright's `test` here and re-export. Generated specs then `import { test }
// from './seed'`.
export {};
