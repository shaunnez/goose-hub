# feature-enhance skill

Read-only feature grounding pass for fresh feature work. It turns product-language intent into candidate repository surfaces before the `factory:grounding` workflow decides whether scouts are needed.

The skill must not diagnose bugs or invent files. Empty file grounding is a valid low-confidence output.
