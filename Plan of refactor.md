# Plan of refactor
plan1-refactor finish

fix: exclude .worktrees from code-quality-metrics.ts globs


m19-fix-plan see below


--> Sonnet 4.6, fresh context, run standalone first.

  Reasons:
  - Bounded refactor. 3 files (context-assembly.ts, new holdout-validator.ts, new
  context-renderer.ts), explicit contracts in the plan, snapshot-test-first approach. Sonnet
  handles mechanical splits cleanly.
  - Doesn't need this session's context. M19.28 is just "extract two functions, preserve
  single-gateway invariant, snapshot-test before/after." Doesn't depend on M19 plan history,
  Codex review iterations, or v6 decisions. Just needs the M19.28 spec + the source files.
  - Cleaner if standalone vs co-located. v6 says "standalone first to avoid double-edit of
  context-assembly.ts." If M19.20 or M19.23 ALSO touches context-assembly, fewer merge
  conflicts if M19.28 lands first as a pure refactor with no semantic change.
  
  What to give the new Sonnet session:
  1. The M19.28 section from m19-fix-plan.md (paste it directly)
  2. ADR 0014 (single-gateway invariant — load-bearing)
  3. Permission to read core/agent-runtime/context-assembly.ts + tests + ESLint rule
  4. Standard gates: snapshot-test BEFORE split, single-gateway preserved, ADR 0014 amended

# Opus time
3. Me (Opus): M19.14 → M19.27 (everything else)

# Parallel
4. Other agent: Plan 2 (grill-and-prd extraction)     ← parallel anytime

# Last codex review architecture stuff
5. See codex

# Maybe do this
https://github.com/shaunnez/goose-hub/issues/597

# Prompt to do it all
if not on main, merge main onto branch then proceed.
Start next M19 issue per CLAUDE.md "Starting the next issue" flow.

  Active milestone: M19: Multi-Agent Orchestration. Filter schedule:current, skip
  factory:in-progress, honour Depends on #N (use parseDependencies in
  core/state-source/dependency-parser.ts), pick lowest-numbered eligible, label
  factory:in-progress immediately.

  M19-specific constraints (read m19-fix-plan.md before touching core/ or slices/):

  1. All new workflows use invokeSkill() from core/agent-runtime/invoke-skill.ts.
     Never direct runtime.run() in new code. Plan 1 SkillInvoker is pre-req and
     already merged.
  2. Holdout discipline: QA and Reviewer never see implementation reasoning,
     decision summaries, or chat history. PR body must not contain reasoning —
     acceptance-criteria checkmarks only. Use agent.decision-summary events.
  3. pipelineRunId propagation: any new event payload in implement/QA/review/retro
     surfaces must carry pipelineRunId. Generated by spec-author (#693), consumed
     by parallel-implement (#694), read by merge-decision (#697).
  4. Real event names only: agent.investigation-complete,
     parallel-implement.wp-committed, parallel-implement.wp-started, qa.completed,
     review.completed. New event kinds (spec.completed, merge-decision.completed,
     pipeline.legacy-fallback, cost.threshold-exceeded, canary.passed/failed)
     must be added to the union in core/event-stream/store.ts.
  5. Real state names only: factory:retrospecting after merge, NOT factory:merged.
     Verify any new state target is legal in core/state-machine/transitions.ts
     before transition call.
  6. New states (factory:spec-ready in #693) hit ~8 files: states.ts,
     transitions.ts, constants.ts, lanes.config.ts, dispatch.ts (resume table),
     apps/web/src/lib/transitions.ts, label-installer.ts. Verify exact paths via
     grep before editing.
  7. Settings precedence: DB project_settings (UI-editable) > project.config.ts
     defaults > skill.config.ts > role defaults. useM19Pipeline, canary, review,
     devReview live in DB.
  8. Vertical slice rule: slices/<name>/ includes only surfaces it touches.
     slice.test.ts and README.md required. No empty ui.tsx for workflow-only
     slices. Slices import core/ via package name @goose-hub/core, never via
     relative paths. Slices never import from other slices.
  9. TDD per CLAUDE.md: failing test first, then implementation, then refactor.
  10. No dogfooding: do NOT run the M19 pipeline against M19 repair issues. Build
      by hand with manual human review per PR.

  Sub-agent dispatch heuristic:
  - Bounded mechanical work (delete fn, refactor split, single-file extract,
    19-site mechanical sweep per #699): dispatch Sonnet 4.6 sub-agent with full
    written brief. Verify diff before commit.
  - Cross-cutting work (state-machine surfaces #693, payload alignment #694,
    schema bumps #697, merge-decision gate #697, dispatcher branches #686/#693/
    #694): keep on main Opus thread. Needs full plan context.

  Pre-PR gate: pnpm typecheck && pnpm test && pnpm build green on both apps
  (server, web). All three. Do not open the PR until all three pass.

  PR body: M<milestone>.<task>: <short description> title under 70 chars.
  Body must contain Closes #N. No implementation reasoning in body.

  After PR opens: update issue body to flip [ ] to [x] for completed acceptance
  criteria, then post the structured transition comment per CLAUDE.md step 10.

  If task contradicts FACTORY_RULES.md, reject with comment citing the violation.
  If ambiguous, check CONTEXT.md, then m19-fix-plan.md, then ask the human. Do
  not guess on architectural decisions.

  Begin by running the gh issue list query.

reading m19-fix-plan.md, CLAUDE.md, FACTORY_RULES.md, CONTEXT.md, then

   Start next M19 issue per CLAUDE.md "Starting the next issue" flow.
  Active milestone: M19: Multi-Agent Orchestration.
  Read m19-fix-plan.md before touching any code.